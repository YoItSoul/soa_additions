# SoA override: Terralith's first-join tellraw ad (Discord/BisectHosting promo)
# is suppressed by shadowing data/terralith/functions/toast.mcfunction with
# this empty function — kubejs data outranks mod datapacks, no extra
# mod/datapack needed. setup.mcfunction still runs (harmless scoreboard).
